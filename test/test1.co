<?
#include "../../../../colang/colang.co"

?><html>
<head>
<title>colang测试</title>
</head>
<body>
<?

	//这个文件用于测试colang的基本语法

	/* 块注释测试 */

	printf("Hello world! 你好\n"/*测试嵌入的注释块*/);

	//测试32位整数定义及赋值
	int a=1;
	int b;
	b=2;
	printf("a=%d\tb=%d\n",a,b);
	//测试常量及变量运算
	a=1+b-3*40/5;
	printf("a=1+b-3*40/5=%d\n",a);

	//测试自增、自减
	a++;
	printf("a++=%d\n",a);
	a--;
	printf("a--=%d\n",a);

	//a+=100;
	//printf("a+=100=%d\n",a);

	//测试if
	if(a==100)
		printf("if a==100\n");
	else
		printf("if a!=100\n");

	int i;
	for(i=0;i<10;i++)
	{
		printf("for i=%d\n",i);
	}

	i=0;
	while(i<10)
	{
		printf("while i=%d\n",i);
		i++;
	}

	i=0;
	while(i<10)
		printf("while i=%d\n",i++);

	i=0;
	do
	{
		printf("do i=%d\n",i);
		i++;
	}while(i<10);

	i=0;
	do
		printf("do i=%d\n",i++);
	while(i<10)

	i=0;
	LABEL1:
		printf("label1\n");
	if(i==0)
	{
		i++;
		goto LABEL1;
	}

	int a1=10;
	a1=15;
	if(a1<10 || a1>20)
		printf("a1<10 || a1>20");
	if(a1>10 && a1<20)
		printf("a1>10 && a1<20");
	
	//void echo(char* str)
	//{
	//	printf(str);
	//	return;
	//}
	//echo("echo\n");

	//int echo2(char* str)
	//{
	//	int r=printf(str);
	//	return r;
	//}

	//int echo3(char* str)
	//{
	//	return printf(str);
	//}

	int echo4(char* str)
	{
		int a=1;

		int i;
		for(i=0;i<10;i++)
		{
			//break;
			//return 0;
		}

	//	if(a==1)
	//		return 0;
	//	else
	//		return printf(str);

		//if(a==1)
		//	a=2;
		//else
		//	a=3;

		return 0;
	}

	int zz=1;
	//if(zz=1)
	//	return 0;

	int zx=2;
?>
</body>
</html>